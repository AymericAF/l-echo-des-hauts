---
/**
 * `/mentions-legales` (§4.2).
 *
 * `noindex` conserve depuis le commit `d369449` : ce lot livre des routes, il n arbitre
 * pas la politique d indexation d une page legale.
 */
import Base from '../layouts/Base.astro';
import PageMentions from '../components/pages/PageMentions.astro';
---

<Base
  titre="Mentions legales"
  page={{ genre: 'statique', locale: 'fr', nom: 'mentions-legales' }}
  noindex
>
  <PageMentions locale="fr" />
</Base>
