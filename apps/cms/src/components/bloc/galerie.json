{
  "collectionName": "components_bloc_galeries",
  "info": {
    "displayName": "Galerie",
    "icon": "images",
    "description": "Serie d images. Une seule legende pour la galerie entiere (A-22). Le carrousel est rendu en CSS, sans JavaScript (A-05). Les alternatives viennent de l alternativeText natif de la Media Library (A-04) — sauf pour les images citees dans `alternatives`, qui les surchargent pour CETTE locale."
  },
  "options": {},
  "attributes": {
    "images": {
      "type": "media",
      "multiple": true,
      "required": true,
      "allowedTypes": [
        "images"
      ]
    },
    "alternatives": {
      "type": "component",
      "repeatable": true,
      "component": "bloc.alternative-image"
    },
    "legende": {
      "type": "string"
    },
    "disposition": {
      "type": "enumeration",
      "enum": [
        "grille",
        "carrousel",
        "pleine-largeur"
      ],
      "required": true,
      "default": "grille"
    }
  }
}
