{
  "collectionName": "components_bloc_encadres",
  "info": {
    "displayName": "Encadre",
    "icon": "exclamation-triangle",
    "description": "Encart lateral. Les titres saisis dans contenu sont rendus en h4 au plus et exclus du sommaire (A-21). Le champ titre n est pas un titre HTML : c est l etiquette de l encart, rendue hors du fil des titres (modele-donnees.md §3.4)."
  },
  "options": {},
  "attributes": {
    "titre": {
      "type": "string"
    },
    "contenu": {
      "type": "blocks",
      "required": true
    },
    "variante": {
      "type": "enumeration",
      "enum": [
        "info",
        "alerte",
        "complement"
      ],
      "required": true,
      "default": "info"
    }
  }
}
